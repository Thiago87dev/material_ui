import { Button, Tooltip } from "@mui/material";

const TooltipComponent = () => {
  return (
    <Tooltip title="Botão com tooltip">
      <Button variant="contained">Botão com tooltip</Button>
    </Tooltip>
  );
};

export default TooltipComponent;
